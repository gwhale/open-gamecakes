// Cake Shift — the ONE place the game's kid-facing name and glyph live.
//
// The name is provisional ("Cake Shift" won over "Cakey Tiers", which a
// six-year-old cannot tell apart from "Cakey Tower" when both sit on the same
// arcade grid; the glyph 🗼 won over 🥞, which would sit next to the 🧇 Sudoku
// booth as two golden-brown breakfasts). If either changes, change it here:
// the registry, launcher, HUD, host, What's New and the end card all read
// these. The URL slug and the skill name stay `hanoi` regardless — they are
// identifiers, not copy, and renaming them would orphan saved attempts.

export const CAKE_SHIFT_NAME = 'Cake Shift';
export const CAKE_SHIFT_GLYPH = '🗼';
/** URL slug AND logic-skill name. Never derived from the display name. */
export const HANOI_SLUG = 'hanoi';
